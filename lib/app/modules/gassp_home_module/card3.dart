import 'package:flutter/material.dart';

class CardTherth extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Center(child: Text('عن الهيئة')),
      ),
    );
  }
}
